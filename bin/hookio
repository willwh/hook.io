#!/usr/bin/env node

var Hook = require('../lib/hookio').Hook;

var hook = new Hook( {
  name: 'the-hook'
});

// We don't need to start a hook if we're generating a scaffold.
if (!hook.init) {
  hook.start();
}
